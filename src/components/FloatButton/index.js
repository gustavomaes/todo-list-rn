import React from 'react'
import ActionButton from 'react-native-action-button';

const FloatButton = props => <ActionButton buttonColor="rgba(68, 138, 255, 1)" onPress={props.onPress} />

export default FloatButton;